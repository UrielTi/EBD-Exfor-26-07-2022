function loadId(url, id) {

    var page = url + id;

    var win = window.open(page, '_blank');
    win.focus();
}
function loadPage(url) {

    var page = url;

    var win = window.open(page, '_blank');
    win.focus();
}